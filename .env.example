# Flask MQTT PDF Demo - Environment Configuration
# Kopieer dit bestand naar .env en pas de waarden aan voor jouw environment

# === FLASK CONFIGURATIE ===
SECRET_KEY=change-me
DEBUG=True
FLASK_ENV=development

# === SERVER CONFIGURATIE ===
# 0.0.0.0 = alle interfaces, 127.0.0.1 = alleen localhost
HOST=0.0.0.0
PORT=5555

# === MQTT BROKER CONFIGURATIE ===
MQTT_BROKER=localhost # Pas aan naar je MQTT broker adres
# Standaard MQTT poort is 1883
MQTT_TCP_PORT=1883
# Veel brokers gebruiken 9001 voor WebSocket over HTTP
MQTT_PORT=9001
# MQTT topic waar berichten naar worden gestuurd en ontvangen
MQTT_TOPIC=demo/messages

# === PDF CONFIGURATIE ===
PDF_DIR=generated